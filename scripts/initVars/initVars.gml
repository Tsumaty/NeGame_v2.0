/// @desc функция перевода переменных в начальное состояние
function initVars()
{
    hp = hpstart;
    x = xstart;
    y = ystart;
    horsp = horspstart;
    versp = verspstart;
    isLookingRight = rightstart;
}