/// @desc функция обновления начальных переменных
function updateStartVars()
{
    hpstart = hp;
    xstart = x;
    ystart = y;
    horspstart = horsp;
    verspstart = versp;
    rightstart = isLookingRight;
}